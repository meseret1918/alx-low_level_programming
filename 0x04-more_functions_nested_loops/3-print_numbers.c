#include "main.h"

/**
 * print_numbers - a function that prints the numberfrom 0 to 9
 *
 * Return: 0-9 followed by a new line
 */
void print_numbers(void)
{
	int z;

	for (z = 48; a <= 57; z++)
	{
		_putchar(z);
	}
	_putchar('\n');
}
